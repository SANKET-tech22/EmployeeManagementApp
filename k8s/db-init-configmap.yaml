apiVersion: v1
kind: ConfigMap
metadata:
  name: db-init-script
  namespace: employee-app
data:
  init.sql: |
    CREATE TABLE employees (
      id SERIAL PRIMARY KEY,
      name VARCHAR(100),
      email VARCHAR(100),
      department VARCHAR(100),
    );